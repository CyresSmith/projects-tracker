import styled from '@emotion/styled';
import theme from 'theme';

export const RightBarBox = styled.aside`
  width: 433px;
  height: 100vh;
  background-color: ${theme.colors.white};
  padding: ${theme.space[4]} ${theme.space[3]};
`;
